name: build jsk
run-name: ${{ github.actor }} is building jsk 🚀
on:
 pull_request:
    branches:
      - staging
    types:
      - closed
    paths:
      - 'jsk/**'        
 workflow_dispatch:
# Some objects are defined but never used lead to warnings when build
# Treating warnings as errors because process.env.CI = true ==> step build jsk exit code 1 and fail
# temporary fix by set process.env.CI = false
env:
  CI: false
jobs:
  build-jsk:
    runs-on: ubuntu-latest
    if: github.event.action == 'closed' && github.event.pull_request.merged == true || github.event_name == 'workflow_dispatch'
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - name: Setup node
        uses: actions/setup-node@v3
        with:
          node-version: '16'
          cache: 'yarn'
      - name: Cache node_modules
        uses: actions/cache@v3.0.1
        with:
          path: '**/node_modules'
          key: ${{ runner.os }}-modules-${{ hashFiles('**/yarn.lock') }}
      - name: Install dependencies
        run: yarn install --prefer-offline
      - name: Run yarn build-lib
        run: yarn build-lib
      - name: Build jsk
        env:
            REACT_APP_LOGIN_URL: ${{ secrets.REACT_APP_LOGIN_URL }}
            REACT_APP_API_URL: ${{ secrets.REACT_APP_API_URL }}
        run: yarn build-jsk
      - name: Save jsk/build folder
        uses: actions/upload-artifact@v3
        with:
          name: jsk-build-folder
          path: jsk/build
          retention-days: 1

  deploy-jsk:
    runs-on: ubuntu-latest
    needs: build-jsk
    steps:
      - name: Download jsk/build folder
        uses: actions/download-artifact@v3
        with:
          name: jsk-build-folder
          path: jsk/build
      - name: Rsync jsk/build folder to Server
        uses: easingthemes/ssh-deploy@v2
        env:
            SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
            ARGS: ""
            SOURCE: "jsk/build"
            REMOTE_HOST: ${{ secrets.REMOTE_HOST }}
            REMOTE_USER: ${{ secrets.REMOTE_USER }}
            TARGET: /home/${{ secrets.REMOTE_USER }}/bworks_2.0/bworks_2.0/jsk

  restart-nginx-service:
    needs: deploy-jsk
    runs-on: ubuntu-latest
    steps:
      - name: Restart nginx service
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/${{ secrets.REMOTE_USER }}/bworks_2.0/bworks_2.0/docker-config
            docker-compose -f compose.yaml restart nginx
      - run: echo "🍏 This job's status is ${{ job.status }}."        
